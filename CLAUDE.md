# Eurograft Website — Claude Instructions

## Git
- **Always work on `main`**, never `master`. The `main` branch is the production branch connected to Vercel.
- Before starting any work, confirm you are on `main`: `git checkout main`
- Push to `origin main` to deploy to production via Vercel's GitHub integration.
